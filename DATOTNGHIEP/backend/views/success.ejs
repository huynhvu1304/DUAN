<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Thanh toán thành công</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css"
      rel="stylesheet"
    />
    <style>
      :root {
        --primary-color: #28a745;
        --secondary-color: #6c757d;
        --text-color: #333;
        --light-text-color: #666;
        --background-color: #f8f9fa;
        --card-background: #ffffff;
        --border-color: #e9ecef;
        --success-bg: #d4edda;
        --success-color: #155724;
        --blue-button-color: #007bff;
        --blue-button-hover: #0056b3;
        --green-button-color: #1c9649;
        --green-button-hover: #0f6b31;
      }

      body {
        font-family: "Poppins", sans-serif;
        background-color: var(--background-color);
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        margin: 0;
        padding: 20px;
        box-sizing: border-box;
      }

      .success-container {
        background-color: var(--card-background);
        padding: 40px;
        border-radius: 12px;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
        text-align: center;
        max-width: 600px;
        width: 100%;
        transition: transform 0.3s ease-in-out;
      }

      .success-container:hover {
        transform: translateY(-5px);
      }

      .success-icon {
        color: var(--primary-color);
        font-size: 80px;
        margin-bottom: 20px;
        animation: fadeInScale 0.8s;
      }

      @keyframes fadeInScale {
        from {
          transform: scale(0.8);
          opacity: 0;
        }
        to {
          transform: scale(1);
          opacity: 1;
        }
      }

      h1 {
        color: var(--text-color);
        font-size: 32px;
        font-weight: 600;
        margin-bottom: 10px;
      }

      p {
        color: var(--light-text-color);
        font-size: 16px;
        line-height: 1.6;
        margin-bottom: 30px;
      }

      .info-section {
        border: 1px solid var(--border-color);
        border-radius: 8px;
        padding: 20px;
        margin-bottom: 25px;
        background-color: #fdfdfd;
      }

      .info-section h2 {
        font-size: 18px;
        font-weight: 600;
        color: var(--text-color);
        margin-bottom: 15px;
        border-bottom: 2px solid var(--border-color);
        padding-bottom: 10px;
        text-align: left;
      }

      .info-row {
        display: flex;
        justify-content: space-between;
        margin-bottom: 12px;
      }

      .info-row span:first-child {
        font-weight: 500;
        color: var(--secondary-color);
      }

      .info-row span:last-child {
        color: var(--text-color);
        font-weight: 500;
      }

      .order-details-list {
        padding: 0;
        list-style: none;
        margin: 0;
      }

      .order-item {
        display: flex;
        align-items: center;
        padding: 15px 0;
        border-bottom: 1px dashed var(--border-color);
      }

      .order-item:last-child {
        border-bottom: none;
      }

      .item-image {
        width: 70px;
        height: 70px;
        object-fit: cover;
        border-radius: 8px;
        margin-right: 20px;
        border: 1px solid var(--border-color);
      }

      .item-info {
        flex-grow: 1;
        text-align: left;
      }

      .item-info h3 {
        font-size: 16px;
        color: var(--text-color);
        margin: 0;
        font-weight: 600;
      }

      .item-info p {
        font-size: 13px;
        color: var(--light-text-color);
        margin: 5px 0 0;
        padding: 0;
      }

      .item-price {
        font-weight: 600;
        color: var(--primary-color);
        font-size: 18px;
      }

      .order-total {
        margin-top: 25px;
        padding-top: 25px;
        border-top: 2px solid var(--border-color);
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 24px;
        font-weight: 700;
        color: var(--primary-color);
        
      }

      .btn-home,
    .btn-order {
        padding: 12px 30px;
        border: none;
        border-radius: 8px;
        font-size: 16px;
        font-weight: 500;
        cursor: pointer;
        text-decoration: none;
        transition: background-color 0.3s, transform 0.2s, box-shadow 0.2s;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.15);
        display: inline-block;
    }

    .btn-home {
        background-color: var(--blue-button-color);
        color: #fff;
    }

    .btn-order {
        background-color: var(--green-button-color);
        color: #fff;
    }

    .btn-home:hover {
        background-color: var(--blue-button-hover);
        transform: translateY(-2px);
        box-shadow: 0 4px 7px rgba(0, 0, 0, 0.2);
    }

    .btn-order:hover {
        background-color: var(--green-button-hover);
        transform: translateY(-2px);
        box-shadow: 0 4px 7px rgba(0, 0, 0, 0.2);
    }

    .button-container {
        display: flex;
        justify-content: center;
        flex-wrap: wrap;
        gap: 15px; /* Tạo khoảng cách giữa các nút */
        margin-top: 30px;
    }
    

      @media (max-width: 600px) {
        .success-container {
          padding: 25px;
        }
        h1 {
          font-size: 26px;
        }
        .info-row,
        .order-total {
          flex-direction: column;
          align-items: flex-start;
        }
        .info-row span:last-child,
        .order-total span:last-child {
          margin-top: 5px;
        }
        .item-image {
          width: 100px;
          height: 100px;
        }
        .order-item {
          flex-wrap: wrap;
        }
        .item-info {
          flex-grow: 0;
          width: calc(100% - 70px);
        }
        .item-price {
          width: 100%;
          text-align: right;
          margin-top: 10px;
        }
        
        .btn-home,
        .btn-order {
            width: 100%; /* Chiếm toàn bộ chiều rộng của container */
        }
      }
    </style>
  </head>
  <body>
    <div class="success-container">
      <i class="bx bx-check-circle success-icon"></i>
      <h1>Thanh Toán Thành Công!</h1>
      <p>
        Cảm ơn bạn đã mua sắm tại cửa hàng của chúng tôi. Đơn hàng của bạn đã
        được tiếp nhận và đang chờ xử lý.
      </p>

      <div class="info-section">
        <h2>Thông tin giao dịch</h2>
        <div class="info-row">
          <span>Mã đơn hàng:</span>
          <span><%= orderCode %></span>
        </div>
        <div class="info-row">
          <span>Trạng thái đơn hàng:</span>
          <span><%= orderStatus %></span>
        </div>
        <div class="info-row">
          <span>Trạng thái thanh toán:</span>
          <span><%= paymentStatus %></span>
        </div>
        <% if (vnp_TransactionNo) { %>
        <div class="info-row">
          <span>Mã giao dịch VNPAY:</span>
          <span><%= vnp_TransactionNo %></span>
        </div>
        <% } %>
      </div>

      <% if (orderDetails && orderDetails.length > 0) { %>
      <div class="info-section">
        <h2>Chi tiết đơn hàng</h2>
        <ul class="order-details-list">
          <% orderDetails.forEach(item => { %>
          <li class="order-item">
            <img
              src="/images/<%= item.productVariantId.image %>"
              alt="Product Image"
              class="item-image"
            />
            <div class="item-info">
              <h3><%= item.productVariantId.productID.name %></h3>
              <p>
                Màu: <%= item.productVariantId.color %>, Kích cỡ: <%=
                item.productVariantId.size %>
              </p>
              <p>Số lượng: <%= item.quantity %></p>
            </div>
            <span class="item-price"
              ><%= (totalAmount || 0).toLocaleString('vi-VN') %>₫</span
            >
          </li>
          <% }); %>
        </ul>
      </div>
      <% } %>

      <div class="order-total">
        <span>Tổng tiền:</span>
        <span><%= (totalAmount || 0).toLocaleString('vi-VN') %>₫</span>
      </div>

     <div class="button-container">
        <a href="<%= clientUrl %>" class="btn-home">Quay về trang chủ</a>
        <a href="<%= clientUrl %>/orders" class="btn-order"
          >Quản lý đơn hàng</a
        >
      </div>
    </div>
  </body>
</html>
